apiVersion: v1
data:
  index.html: |
    Nginx C
kind: ConfigMap
metadata:
  name: nginx-deployment-c
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    openshift.io/host.generated: "true"
  labels:
    app: nginx-c
  name: nginx-deployment-c
spec:
  host: nginx-deployment-c-application.apps.akaris-osc.rhos-d.infra.prod.upshift.rdu2.redhat.com
  port:
    targetPort: 80
  to:
    kind: Service
    name: nginx-deployment-c
    weight: 100
  wildcardPolicy: None
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-c
  name: nginx-deployment-c
  namespace: application
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-c
  sessionAffinity: None
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-c
  labels:
    app: nginx-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-c
  template:
    metadata:
      labels:
        app: nginx-c
    spec:
      containers:
      - name: nginx-c
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config-volume
          mountPath: /usr/share/nginx/html
      volumes:
        - name: config-volume
          configMap:
            name: nginx-deployment-c
